'use strict';

module.exports = {
  'arbeidsflyt-logg': { schema: require('./arbeidsflyt-logg') },
  'planlagt-publisering': { schema: require('./planlagt-publisering') },
  varsling: { schema: require('./varsling') },
};
