# Server
HOST=0.0.0.0
PORT=1337

# Secrets (generate unique values for each environment)
# You can generate secrets with: openssl rand -base64 32
APP_KEYS=toBeGenerated1,toBeGenerated2,toBeGenerated3,toBeGenerated4
ADMIN_JWT_SECRET=toBeGenerated
API_TOKEN_SALT=toBeGenerated
TRANSFER_TOKEN_SALT=toBeGenerated
JWT_SECRET=toBeGenerated

# Database (PostgreSQL)
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=kinorge_dev
DATABASE_USERNAME=your_username
DATABASE_PASSWORD=

# Preview
FRONTEND_URL=http://localhost:4321
PREVIEW_SECRET=change-me

# Database (SQLite alternative — uncomment to use instead of PostgreSQL)
# DATABASE_CLIENT=sqlite
# DATABASE_FILENAME=.tmp/data.db
