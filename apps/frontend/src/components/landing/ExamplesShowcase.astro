---
import { Heading, Paragraph } from '@digdir/designsystemet-react';

interface Example {
  title: string;
  organization: string;
  slug: string;
  image?: string;
  description?: string;
}

interface Props {
  examples?: Example[];
}

const { examples = [] } = Astro.props;

const displayExamples: Example[] = examples.length > 0 ? examples : [
  { title: 'KI hos Oslo universitetssykehus', organization: 'Ullensag', slug: 'oslo-saksbehandling', description: 'Vi bruker maskinlæring og bildegjenkjenning for raskere og bedre diagnostisering.' },
  { title: 'Kunnskapsassistenten', organization: 'Digitaliseringsdirektoratet', slug: 'kunnskapsassistenten', description: 'Vi har bygget en KI-assistent som hjelper med å finne og sammenstille kunnskap i utredningsprosesser.' },
  { title: 'Spacemaker', organization: 'Virksomhet', slug: 'sykehus-radiologi', description: '' },
];

const tabs = [
  { label: 'Se alle eksempler', active: true },
  { label: 'Offentlige virksomheter', active: false },
  { label: 'Private virksomheter', active: false },
];
---

<section class="examples-showcase">
  <div class="examples-showcase-container">
    <div class="examples-showcase-header">
      <Heading level={2} size="xlarge" className="examples-showcase-heading">
        Se hvordan andre har brukt KI
      </Heading>
      <Paragraph size="medium" className="examples-showcase-subtext">
        Finn inspirasjon og lær av andre.
      </Paragraph>
    </div>

    <div class="examples-showcase-tabs">
      {tabs.map((tab) => (
        <button
          class:list={['examples-tab', { 'examples-tab--active': tab.active }]}
          type="button"
        >
          {tab.label}
        </button>
      ))}
    </div>

    <div class="examples-showcase-grid">
      {displayExamples.map((example) => (
        <a href={`/eksempler/${example.slug}`} class="showcase-card">
          <div class="showcase-card-image-wrapper">
            {example.image ? (
              <img src={example.image} alt="" loading="lazy" class="showcase-card-img" />
            ) : (
              <div class="showcase-card-placeholder"></div>
            )}
          </div>
          <div class="showcase-card-divider"></div>
          <div class="showcase-card-content">
            <span class="showcase-card-org">{example.organization}</span>
            <Heading level={3} size="xsmall" className="showcase-card-title">
              {example.title}
            </Heading>
            {example.description && (
              <Paragraph size="small" className="showcase-card-description">
                {example.description}
              </Paragraph>
            )}
          </div>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
  .examples-showcase {
    padding: var(--ds-size-12) var(--ds-size-4);
  }

  .examples-showcase-container {
    max-width: 1280px;
    margin: 0 auto;
  }

  .examples-showcase-header {
    margin-bottom: var(--ds-size-6);
  }

  :global(.examples-showcase-heading) {
    margin: 0 0 var(--ds-size-4);
  }

  :global(.examples-showcase-subtext) {
    color: var(--ds-color-neutral-text-subtle);
    margin: 0;
  }

  .examples-showcase-tabs {
    display: flex;
    flex-wrap: wrap;
    gap: var(--ds-size-2);
    margin-bottom: var(--ds-size-8);
  }

  .examples-tab {
    display: inline-flex;
    align-items: center;
    padding: var(--ds-size-2) var(--ds-size-4);
    font-size: var(--ds-font-size-1);
    font-weight: 300;
    border: 2px solid rgba(0, 0, 0, 0.15);
    border-radius: 11px;
    background-color: #ffffff;
    color: #000000;
    cursor: pointer;
    transition: all 0.15s ease;
    font-family: inherit;
  }

  .examples-tab:hover {
    border-color: rgba(0, 0, 0, 0.3);
  }

  .examples-tab--active {
    border-color: rgba(0, 0, 0, 0.3);
    font-weight: 400;
  }

  .examples-showcase-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--ds-size-6);
  }

  @media (min-width: 640px) {
    .examples-showcase-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .examples-showcase-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .showcase-card {
    display: flex;
    flex-direction: column;
    background-color: #E2F0FC;
    border-radius: 8px;
    overflow: hidden;
    text-decoration: none;
    color: inherit;
    transition: box-shadow 0.2s ease;
  }

  .showcase-card:hover {
    box-shadow: var(--ds-shadow-md);
  }

  .showcase-card-image-wrapper {
    display: flex;
    justify-content: center;
    padding: 24px;
    padding-bottom: 0;
  }

  .showcase-card-img {
    width: 90%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    display: block;
    border-radius: 24px;
  }

  .showcase-card-placeholder {
    width: 90%;
    aspect-ratio: 16 / 9;
    background-color: #D9D9D9;
    border-radius: 24px;
  }

  .showcase-card-divider {
    width: 90%;
    height: 1px;
    background-color: #D9D9D9;
    margin: 24px auto 0;
    border-radius: 0.5px;
  }

  .showcase-card-content {
    display: flex;
    flex-direction: column;
    gap: var(--ds-size-1);
    padding: var(--ds-size-5);
  }

  .showcase-card-org {
    color: var(--ds-color-neutral-text-subtle);
    font-size: var(--ds-font-size-0);
    font-weight: var(--ds-font-weight-medium);
  }

  :global(.showcase-card-title) {
    margin: 0;
    line-height: 1.3;
  }

  :global(.showcase-card-description) {
    color: var(--ds-color-neutral-text-subtle);
    margin: 0;
    line-height: 1.5;
    font-size: 0.8125rem;
  }
</style>
